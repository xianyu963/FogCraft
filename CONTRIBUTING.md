# 🤝 贡献指南

感谢你愿意为 FogCraft 贡献力量！本项目是一个纯静态、单文件的 Minecraft 物品猜谜游戏，贡献方式主要分为**数据贡献**与**代码贡献**两类。

## 🧩 贡献新物品数据

Minecraft 每次更新都会带来新物品，自动更新脚本会尝试处理，但人工核对更可靠：

1. **准备图标**：将物品图标 PNG（128×128 像素）放入 `item/`，文件名使用英文 ID（如 `new_item.png`）
2. **补充中文名**：在 `items_zh.txt` 末尾追加一行，格式为 `英文id=中文名`（一行一条，保持 CRLF 行尾）
3. **生成线索**：运行 `node fetch-clues.js` 重新生成 `data/clues.json`（每个物品 6 条线索；若自动生成的线索质量不佳，可人工编辑 `data/clues.json` 中对应条目）
4. **重建游戏数据**：运行 `node build.js`，将数据注入 `index.html`
5. **验证**：
   - 运行 `node update-data.js` 确认无异常
   - 本地 `node server.js` 启动后，确认新物品可被猜中、线索可解锁、图标正常显示
6. 提交时请附上新物品清单与来源链接（如 Wiki 页面）

## 💻 贡献代码

1. **Fork 仓库**并创建特性分支（`feat/你的改动`）
2. 遵循现有代码风格：
   - 单文件 `index.html` 内完成改动（CSS 追加不覆盖、函数加 JSDoc 注释）
   - 新增功能请同步更新 README.md 的功能表
3. 提交前自测：
   - 四模式（标准 / 每日 / 竞速 / 拼字）均可正常游玩
   - 桌面端与移动端（375px 宽）布局无错位
   - 控制台无报错
4. 发起 Pull Request，说明改动内容与测试方式

## 📝 线索质量规范

贡献线索时请遵循以下硬规则：

- 每条线索**必须包含具体数值或具体物品名**（如"需要 4 个铁锭合成"），禁用"万能句"
- 禁用词：杂项、特殊功能、用途多样、实用物品、多种配方、常见物品、基本物品、普通物品、各类、各种、装饰用途
- 同纹组（贴图相同）物品：前 3 条可共享，后 3 条必须差异化（如涂蜡 / 虫蚀）
- 线索要"猜得出来又不会秒杀"：避免直接出现物品名（唯一锁定句除外）

## 🐛 报告问题

提交 Issue 时请包含：浏览器与版本、设备（移动/桌面）、复现步骤、控制台报错信息。
